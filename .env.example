API_BASE_URL=http://localhost:8000
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI. Without a key the pipeline runs its deterministic path only: rules
# classify, labels extract, and anything undecided is escalated for review.
GEMINI_API_KEY=
# Optional model override (defaults to a current Gemini flash model).
GEMINI_MODEL=
